# 基础设施与数据库环境准备 — 任务清单（进度追踪）

> **用途**：在接入 Spring / Flyway 业务代码前，完成 **MySQL + PostgreSQL（pgvector）+ Redis** 的安装、配置与 DDL 验证。  
> **DDL 权威来源**：`docs-back/三层记忆DDL设计文档-Redis+PostgreSQL+MySQL.md`  
> **向量扩展参考**：`docs-back/向量数据库配置指导文档-PostgreSQL与pgvector.md`

---

## 使用说明

- 每条任务前用 `- [ ]` / `- [x]` 勾选，或填「完成日期」「备注」。
- **Phase 总览表**在文末，用于一眼看整体进度。

---

## Phase A — 规划与账号安全

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| A-1 | 确定部署方式：本机安装 / Docker Compose / 云 RDS | 书面记录选型 | - [ ] |
| A-2 | 确定环境名：`local` / `dev`，与应用 `spring.profiles` 对齐 | 与后续 `application-*.yml` 一致 | - [ ] |
| A-3 | 为 MySQL、PostgreSQL 各建**专用业务账号**（禁止应用长期使用 root） | 账号最小权限原则 | - [ ] |
| A-4 | 密码与连接串：**不入库 Git**，使用环境变量或本地 gitignore 文件 | 仓库无明文密码 | - [ ] |
| A-5 | 约定**时区**（建议应用与 DB 统一 UTC 或 `Asia/Shanghai`） | 文档写明 | - [ ] |

---

## Phase B — MySQL（主链路：用户、会话、消息、审计、冷记忆）

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| B-1 | 安装或拉起 MySQL 实例（建议 8.0+） | `mysql --version` 或容器 healthy | - [ ] |
| B-2 | 创建业务库（例：`q_ai_agent`），字符集 **utf8mb4** | `SHOW CREATE DATABASE` 确认 | - [ ] |
| B-3 | 创建业务用户并授权（仅该库 DDL/DML 所需权限） | 非 root 可连接 | - [ ] |
| B-4 | 配置 `max_connections` / `innodb_buffer_pool`（本地可默认，记录即可） | 有基本参数说明 | - [ ] |
| B-5 | 执行 DDL 文档中 **MySQL** 段：`users` → `chat_session` → `chat_message` → `tool_call_audit` → `cold_memory_transcripts`（注意外键顺序） | 无报错，`SHOW TABLES` 齐全 | - [ ] |
| B-6 | 验证唯一约束：`chat_id`、`message_id`、`(chat_id,seq_no)` | 故意插入重复应失败 | - [ ] |
| B-7 | （可选）准备 Flyway 占位：后续迁移脚本目录约定 | 与开发任务清单 P1 对齐 | - [ ] |

---

## Phase C — PostgreSQL + pgvector（温记忆 / RAG）

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| C-1 | 安装或拉起 PostgreSQL（建议 15+）；**优先使用带 pgvector 的发行版/镜像** | `psql --version` | - [ ] |
| C-2 | 创建业务库（例：`agent_warm`） | 可连接 | - [ ] |
| C-3 | 创建业务用户并授权 | 非超级用户可建 schema/表（按你授权策略） | - [ ] |
| C-4 | 在目标库执行：`CREATE EXTENSION IF NOT EXISTS vector;` | `\dx` 列出 `vector` | - [ ] |
| C-5 | 执行 DDL 文档中 **PostgreSQL** 段：`agent_core` schema、`memory_topic_index`、`memory_topic_content` | 无报错，`\dt agent_core.*` 可见 | - [ ] |
| C-6 | 确认向量维度与后续 embedding 模型一致（文档为 **1536**，若改模型需同步改列） | 书面记录模型名与维度 | - [ ] |
| C-7 | （可选）创建 HNSW 向量索引（数据量大再上；首期可仅建表） | 与 DDL 注释一致 | - [ ] |

---

## Phase D — Redis（热记忆）

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| D-1 | 安装或拉起 Redis（建议 6+） | `redis-cli ping` → `PONG` | - [ ] |
| D-2 | 配置访问：绑定地址、端口（默认 6379）、**密码**（非本地建议必开） | 客户端可鉴权连接 | - [ ] |
| D-3 | 了解持久化策略：RDB/AOF（本地可默认，生产需定案） | 简要记录 | - [ ] |
| D-4 | 确认 `maxmemory` 与淘汰策略（热记忆为缓存，需可接受淘汰或足够内存） | 与 DDL 文档 §5 一致的可运维性 | - [ ] |
| D-5 | 手工验证：String / List 读写（对应 `agent:hot:global:*`、`agent:hot:session:*`） | SET/LPUSH/LTRIM 正常 | - [ ] |

---

## Phase E — 网络、防火墙与连接串

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| E-1 | 确认应用运行位置能访问：MySQL 端口、PG 端口、Redis 端口 | telnet 或客户端连通 | - [ ] |
| E-2 | 整理三条 JDBC/Redis URL（**仅本地 secrets**） | 一份 `local-env.example`（无真实密码）可选 | - [ ] |
| E-3 | Docker 场景：固定 service 名、端口映射、数据卷挂载 | 重启容器数据不丢（按需） | - [ ] |

---

## Phase F — 验证与文档收口

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| F-1 | **连通性三件套**：MySQL `SELECT 1`；PG `SELECT 1`；Redis `PING` | 一次性截图或记录 | - [ ] |
| F-2 | DDL 与文档对照：表名、字段、索引无遗漏 | 自检表打勾 | - [ ] |
| F-3 | 更新团队可见的「本地启动说明」（README 或本文附录） | 新人可按文档复现 | - [ ] |
| F-4 | 在 `MEMORY.md` 文档索引中已收录本清单（若采用本仓库流程） | 索引可查 | - [ ] |

---

## Phase G — （可选）容器一键环境

| ID | 任务 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| G-1 | 编写 `docker-compose.yml`：mysql + postgres(pgvector) + redis | `docker compose up -d` 成功 | - [ ] |
| G-2 | 数据目录 volume 命名与备份策略 | 文档说明 | - [ ] |

---

## 进度总览（Phase 级）

| Phase | 说明 | 完成 |
| --- | --- | --- |
| A | 规划与安全 | - [ ] |
| B | MySQL | - [ ] |
| C | PostgreSQL + pgvector | - [ ] |
| D | Redis | - [ ] |
| E | 网络与连接串 | - [ ] |
| F | 验证与收口 | - [ ] |
| G | （可选）Docker Compose | - [ ] |

---

## 附录：与后续开发的衔接

环境就绪后，下一跳通常是：

1. Spring Boot 引入 `spring-boot-starter-jdbc`（MySQL）、第二数据源或独立客户端（PG）、Redis Starter。  
2. Flyway 按 `简化实现方案-开发任务清单与进度追踪.md` 的 **P1** 落地迁移脚本。
