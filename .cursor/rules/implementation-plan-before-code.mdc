---
description: 写实现代码前先输出方案；待用户确认后再动手改代码
alwaysApply: true
---

# 先方案后代码（需用户确认）

## 执行顺序（强制）
1. **在首次对仓库做实现性改动之前**（新增/修改业务或配置代码、非纯文档），先输出 **实现方案**，至少包含：
   - 目标与 **非目标**（范围边界）；
   - 拟改 **文件/类清单** 与职责；
   - 关键设计取舍（1～3 点）；
   - **验证方式**（编译、测试、手工步骤）。
2. **在用户明确同意后再开始写代码**。认可表述包括但不限于：`可以开始`、`按方案执行`、`OK 实现`、`没问题`；若用户只提问或要求继续讨论方案，**不进入实现**。
3. 用户已预先声明「直接实现、无需再问」时，可在同轮内输出 **极简方案（3～5 行）** 后立即实现，但仍需保留边界与验收一句。

## 例外（可直接小改）
- 仅修正错别字、格式、明显笔误；
- 用户粘贴了完整补丁并明确要求应用；
- 纯 **只读** 操作（搜索、阅读、解释代码）。

## 反例
- 未等确认就提交大段 diff。
- 方案与最终代码改动范围严重不一致且不说明。
