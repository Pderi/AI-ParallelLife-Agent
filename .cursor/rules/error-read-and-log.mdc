---
description: 生成前读取项目根目录 ERROR.md，犯错后必须立即记录（详见 three-layer-memory-error-workflow）
alwaysApply: true
---

# Error 文档读取与记录规则

## 执行顺序（强制）
1. 每次生成任何内容前，先读取项目根目录 `ERROR.md`（与 `MEMORY.md` 同级）。
2. 读取后再开始分析、编写、修改或输出。
3. 一旦发现本次生成存在错误（逻辑错误、规则违背、遗漏、格式错误等），必须立即补充到 `ERROR.md`。

## 记录要求
- 每条错误至少包含：场景、错误现象、根因分析、修复动作、预防措施、状态。
- 若同类错误重复出现，必须在 `ERROR.md` 的检查清单中新增对应检查项。

## 禁止项
- 未读取 `ERROR.md` 前直接开始生成正式内容。
- 发现错误后不记录或延迟记录。
