---
description: 明确优先代码质量而非速度；过载任务必须拆分并逐步验证
alwaysApply: true
---

# 质量优先于速度（执行纪律）

## 核心
- **优先保证正确性、可读性、可维护性与可验证性**，其次才是单次回复速度或改动规模。
- 当任务涉及多文件、多阶段或高风险操作时：**自行拆成小任务**，**按顺序执行**；每步有可验证的完成标准，再进入下一步。
- 与 `.cursor/rules/quality-first-incremental-delivery.mdc` **配合使用**：该文件定义拆分模板与反例；本规则强调 **不因赶进度跳过验证或合并无关改动**。

## 执行要求
1. 评估工作量：若一步内难以保证质量（含测试/编译检查），**主动拆分**并说明顺序。
2. 不为「一次说完」而省略边界说明、错误处理约定或与项目异常体系的一致性。
3. 并发工具调用仅用于**无依赖**的只读探测；有顺序依赖的写操作 **串行** 并每步可回滚或可追溯。

## 反例
- 「先全部改完再测」导致大量返工。
- 为省 token 省略关键分支或复制粘贴未适配项目的代码。
