---
description: 减少冗余 try-catch，优先统一异常语义
alwaysApply: true
---

# 异常处理约束（全局）

## 目标
- 减少分散且重复的 `try-catch` 结构，降低噪音与维护成本。
- 优先使用统一异常体系（如 `BusinessException` + `ErrorCode`）表达错误语义。

## 执行规则
1. 先做参数校验和前置条件校验，失败直接抛业务异常，不返回拼接错误字符串。
2. 同一业务路径只保留必要的“单点异常转换”，避免多层嵌套 `try-catch`。
3. 对可预期业务错误使用明确错误码；对未知系统错误统一转换为内部错误。
4. 不在底层吞异常；记录必要日志后继续向上抛出，由全局异常处理器统一输出。
5. 仅在必须处理中断语义（如线程中断）时做特例处理。
