---
description: 生成内容前先读 MEMORY，生成后必须同步更新 MEMORY
alwaysApply: true
---

# Memory 优先与同步规则

## 执行顺序（强制）
1. 每次开始生成任何内容前，先读取当前项目根目录的 `MEMORY.md`。
2. 读取后再进行分析、规划、写作或代码生成。
3. 每次完成输出后，必须检查是否产生了新的上下文变化。
4. 若有变化（新增文档、结论变更、路径变化、规则变化、技术决策变化），立即更新 `MEMORY.md`。

## 更新要求
- `MEMORY.md` 必须至少包含：项目定位、当前业务结论、文档索引、最近更新记录。
- 文档索引必须保持可用路径，新增文档后立即补录。
- 如果本次输出未引入任何上下文变化，也应确认无需更新后再结束。

## 约束
- 未读取 `MEMORY.md` 前，不允许直接开始生成正式内容。
- 完成内容后不允许跳过 memory 同步步骤。
