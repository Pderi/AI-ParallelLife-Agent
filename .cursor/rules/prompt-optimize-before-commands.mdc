---
description: 执行终端/破坏性操作前，先优化并重述用户意图再行动
alwaysApply: true
---

# 提示词优化后再执行命令

## 执行顺序（强制）
1. **在运行终端命令、批量修改文件、或执行可能改变仓库/环境状态的操作之前**，先用简短文字完成 **提示词优化**：补全隐含目标、约束、验收标准、相关路径或技术栈。
2. 向用户展示 **优化后的理解**（可列表 3～7 条），再执行具体操作；若用户原句已极度明确，仍需用 1～2 句确认边界（如环境、分支、是否落库）。

## 优化检查清单（按需选用）
- 目标：要解决什么问题 / 交付什么？
- 范围：哪些模块、文件、接口在范围内；明确 **不做** 什么。
- 约束：版本、性能、安全、与 `MEMORY.md` / 既定架构是否一致。
- 验收：如何算完成（编译、测试、手工步骤）。

## 反例
- 未澄清歧义就直接 `git reset`、`rm`、`mvn deploy`、大范围替换。
- 把用户一句话当成完整需求直接开改，省略对目标与风险的复述。

## 良好示例
```text
你的需求可理解为：在 X 类中增加 Y 行为，且不改动 Z。我将先只读相关文件再提议改动；确认后执行。
接下来执行：……
```
