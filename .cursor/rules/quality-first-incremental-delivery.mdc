---
description: 生成代码时优先质量，复杂内容拆分小任务逐步交付
alwaysApply: true
---

# 质量优先与渐进式交付

## 核心原则
- 先保证正确性、可维护性与可验证性，再追求一次性产出规模。
- 需求复杂或改动面较大时，不一次性铺开，先拆成小任务逐步完成。
- 每个小任务都应可独立验证，并明确“完成标准”。

## 执行要求
1. 在动手前先确认边界、依赖、风险和验收方式。
2. 优先交付最小可用改动（MVP），然后迭代增强。
3. 单次改动尽量聚焦一个目标，避免在同一批次混入无关重构。
4. 每一步完成后进行基本验证（编译、测试、关键路径检查）。
5. 若上下文过大或信息不足，先输出分步计划并按步骤推进，不强行一次完成。

## 任务拆分模板
1. 目标定义：本步解决什么问题。
2. 改动范围：涉及哪些文件或模块。
3. 验证方式：如何证明本步有效。
4. 下一步：剩余工作与风险点。

## 反例约束
- 不为追求“看起来完成”而跳过验证。
- 不在单次输出中塞入过多未验证的大改动。
- 不在需求不清时直接生成大段实现代码。
