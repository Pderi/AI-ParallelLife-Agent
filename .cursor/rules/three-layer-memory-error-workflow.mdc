---
description: 三层记忆（MEMORY/docs/代码）+ ERROR 纠错；生成前后必读必更
alwaysApply: true
---

# 三层记忆与纠错工作流（全局）

## 三层记忆在本项目中的含义

| 层级 | 载体 | 用途 |
| --- | --- | --- |
| **热记忆（Hot）** | 项目根目录 `MEMORY.md` | 文档索引、项目结论、最近更新；**每次生成前必读**，用于快速对齐上下文。 |
| **温记忆（Warm）** | `docs-front/`、`docs-back/` | 领域说明、接口与设计文档；**按需阅读**与当前任务相关的文件，勿整库塞进上下文。 |
| **冷记忆（Cold）** | 完整代码库（如 `src/`） | 实现与核对事实的来源；**按需检索/打开**，不假设已读完整个仓库。 |

## 纠错机制（Error）

- 项目根目录 **`ERROR.md`**：记录在生成内容过程中出现的错误、根因、修复动作与预防措施；用于反思与避免重复犯错。
- 若仓库中尚无 `ERROR.md`，可按既有模板创建后再记录。

## 强制工作流程

1. **生成任意正式内容前（代码、文档、方案）**  
   - 先读取 **`MEMORY.md`**  
   - 再读取 **`ERROR.md`**  
   - 然后再分析、规划或输出。

2. **生成完成后**  
   - 若有项目上下文变化（新文档、路径、决策、待办变更）：**立即更新 `MEMORY.md`**（摘要、索引、待办等）。  
   - 若本次存在错误或规则违背：**立即写入 `ERROR.md`**（不在会话结束后补记）。

3. **若本次无任何上下文变化且无错误**  
   - 仍需快速确认 `MEMORY.md` / `ERROR.md` 无需修改后再结束。

## 约束

- 未读完 `MEMORY.md` 与 `ERROR.md`，不得开始输出正式方案或代码。  
- 禁止跳过生成后的同步与纠错记录步骤。
