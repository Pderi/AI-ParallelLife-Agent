---
description: 生成内容前阅读MEMORY，完成后更新MEMORY
alwaysApply: true
---

# MEMORY 工作流规则（全局）

## 执行要求
1. 在生成任何内容（代码、文档、方案）前，必须先读取项目根目录 `MEMORY.md`。
2. 输出内容时，需与 `MEMORY.md` 中的项目现状、优先级和文档索引保持一致。
3. 完成内容生成后，必须同步更新 `MEMORY.md`：
   - 更新“最近已完成改造（摘要）”
   - 更新“当前待办（高优先）”
   - 若新增文档，补充到文档索引
4. 如果本次改动不影响项目上下文，仍需确认 `MEMORY.md` 无需变更并保持一致性。

## 质量约束
- `MEMORY.md` 记录必须简洁、可验证、面向事实。
- 不写未落地的结果，不写与代码状态不一致的信息。
